group = 'kaba.plugins.gradle'
version = '0.1'

apply {
    from script('groovy')
    from script('publish')
}

dependencies {
    compile localGroovy()
    compile gradleApi()

    testCompile libraries.junit
    testCompile libraries.spock
}
