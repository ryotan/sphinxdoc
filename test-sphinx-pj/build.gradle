apply plugin: 'sphinxdoc'

wrapper {
    gradleVersion = '1.8'
}

sphinxdoc {
    executable = '/usr/local/bin/sphinx-build'
    out = 'build/build-test'
    settings = [version: '1.3.0', release: '1.3.0 b1']
}
